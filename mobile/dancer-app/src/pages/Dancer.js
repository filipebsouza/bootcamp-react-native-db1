import React from 'react';
import { View, Text } from 'react-native';

import ButtonBack from '../components/ButtonBack';

export default function Dancer({ navigation }) {
  return <ButtonBack navigation={navigation}></ButtonBack>
}